name: statusbarz
description: A Flutter package for dynamically changing status bar color
version: 1.0.0
homepage: https://mapventure.com

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
